{
  "name": "remiel",
  "private": true,
  "scripts": {
    "dev:bot": "pnpm --filter remiel-bot dev",
    "dev:server": "pnpm --filter remiel-server dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
